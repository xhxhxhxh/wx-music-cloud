<x-bottom-modal showModal="{{showModal}}" bind:closeModal="closeModal">
  <view slot="modal-content">
    <button class="authorize"
      open-type="getUserInfo"
      bindgetuserinfo="onGotUserInfo"
    >获取微信授权信息</button>
  </view>
</x-bottom-modal>
