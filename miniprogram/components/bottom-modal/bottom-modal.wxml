<view class="modal" hidden="{{!showModal}}">
  <view class="panel" style="bottom: {{bottomStyle}}">
      <i class="iconfont icon-shanchu1" bind:tap="onClose"></i>
      <!-- slot插槽  具名插槽-->
      <slot name="modal-content"></slot>
  </view>
</view>
